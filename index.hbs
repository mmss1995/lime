{{!< default}}

<main class="container">
    <div class="columns is-multiline">
        {{#foreach posts}}
        <div class="column is-half is-centered">
        </div>
        {{> post_card}}
        {{/foreach}}
    </div>
</main>

<!-- Pagination-->
<div class="container">
{{pagination}}

</div>