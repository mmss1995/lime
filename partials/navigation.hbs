{{#foreach navigation}}
<li class="nav-item{{#if current}} active{{/if}}"><a href="{{url absolute="true"}}">{{label}}
        {{#if current}}
        <span class="sr-only">(current)</span>
        {{/if}}
    </a></li>
{{/foreach}}